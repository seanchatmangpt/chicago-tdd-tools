# Complete Combinatorial-Maximalism Refactor

> Manufactured constitutional guide. Do not edit.

## Governing calculus

`exact observation → admitted semantic model → bounded candidate lattice → deterministic composition → verification → explicit authorization → brokered actuation → observed consequence → causal receipt → independent replay`

The refactor makes the existing Chicago TDD modules realizations of one pure shared kernel rather than competing architectural authorities. Existing APIs remain compatible, but architecture selection, external publication, authority, receipts, and replay are now governed by the generated lattice.

## Internal maximalism

Internal maximalism maximizes lawful reuse across implementations, runtimes, storage modes, protocols, projections, and verification rails. The kernel explores combinations without filesystem, process, network, provider-SDK, credential, or deployment authority.

## External maximalism

External maximalism publishes explicit consumer contracts for every external realization. Every contract names its protocol, authority class, receipt requirement, replay requirement, and private-dependency exclusion. External utility never implies internal authority leakage.

## Projection law

Every selected realization projects across ten orthogonal axes:

`strategy × process × role × data × application × technology × control × evidence × deployment × consumer`

The projection product is bounded to 512 consequences. Maximalism is therefore combinatorial and lawful rather than unbounded output volume.

## Standing law

`Candidate ≠ Verified ≠ Authorized ≠ Actuated ≠ Receipted`

The pure kernel can manufacture candidates, verify evidence, validate grants, and emit broker intents. It cannot execute a side effect. Only an observed broker consequence can be admitted into a BLAKE3 receipt. Replay independently compares the receipt against a later observation.

## Pack law

`Pack = SemanticKernel + Operators + Projections + Contract + Lifecycle + Evidence`

This cell is the canonical architecture pack. The pre-existing standards cell is its constitutional pack. The legacy root operator-registry ggen manifest remains an adapter until every consumer migrates to profile-driven composition.

## Verification ladder

`unit → integration → e2e → chaos → stress → benchmark → verifier report`

The standalone generated crate proves the pure kernel independently of the parent workspace. The receiver workflow additionally executes exact-head validation, real pinned ggen projection, BLAKE3 receipt closure, deterministic second sync, and the sabotage suite.
