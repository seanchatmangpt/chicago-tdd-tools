# Makefile.toml for chicago-tdd-mcp
# Minimal stubs so cargo make workspace iteration does not fail.

[tasks.lint]
description = "Run clippy on chicago-tdd-mcp"
command = "cargo"
args = ["clippy", "--manifest-path", "Cargo.toml", "--", "-D", "warnings"]

[tasks.fmt]
description = "Format chicago-tdd-mcp"
command = "cargo"
args = ["fmt", "--manifest-path", "Cargo.toml"]

[tasks.test-unit]
description = "Run chicago-tdd-mcp tests"
command = "cargo"
args = ["test", "--manifest-path", "Cargo.toml"]

[tasks.docs-check]
description = "No-op: chicago-tdd-mcp has no mdbook documentation"
script = ["echo '✅ docs-check: skipped for chicago-tdd-mcp'"]

[tasks.spec]
description = "No-op for chicago-tdd-mcp"
script = ["echo '✅ spec: skipped for chicago-tdd-mcp'"]

[tasks.spec-check]
description = "No-op for chicago-tdd-mcp"
script = ["echo '✅ spec-check: skipped for chicago-tdd-mcp'"]
