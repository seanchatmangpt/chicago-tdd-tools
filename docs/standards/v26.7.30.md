# Engineering Standards v26.7.30

This retrofit admits the engineering law developed from July 24 through July 30, 2026 without replacing the existing Chicago TDD operator-registry rail.

## Preserved system

The existing crate, 43 YAWL patterns, OCEL support, governance channels, and root `ggen.toml` remain authoritative for their current surfaces. The constitutional cell is additive and independently executable.

## Constitutional cell

`standards/chicago-tdd/` is a bounded graph-to-artifact manufacturing cell:

- `O.star.toml` admits the observation, authority fence, exact toolchain pins, Gall checkpoint chain, and receipt components.
- `ontology.ttl` is the semantic source of truth.
- `ggen.toml` declares SPARQL extraction and generated ownership.
- `templates/` defines deterministic projections.
- `generated/` contains products and an isolated Rust verifier crate.
- `scripts/verify_ggen_standards.py` independently recomputes every generated byte, enforces 22 gates, performs atomic writes, proves second-sync and receipt identity, emits OCEL 2.0 checkpoint evidence, and closes the receipt chain.
- `scripts/sabotage_ggen_standards.py` executes 11 negative witnesses and requires stable typed refusals.

## Next ten Gall checkpoints

| ID | Boundary | Consequence |
|---|---|---|
| `GALL-013` | observation provenance | Exact O* identity and seven-day source window |
| `GALL-014` | checkpoint totality | Ten unique contiguous checkpoint resources |
| `GALL-015` | dependency graph | One immediate-predecessor chain from `GALL-012` |
| `GALL-016` | standing | Authored plans remain `ADMITTED`; checkpoint is not crown |
| `GALL-017` | evidence | Every checkpoint has receipt, evidence, and falsifier recipes |
| `GALL-018` | resolution | ggen, LSP, wasm4pm, and Rust revisions are exact |
| `GALL-019` | rendering | Authored and generated paths are disjoint |
| `GALL-020` | receiver | Evidence binds one exact 40-hex commit |
| `GALL-021` | process evidence | One OCEL 2.0 object and event per checkpoint |
| `GALL-022` | replay | Repeated receipt manufacture is byte-identical |

These resources are projected by real ggen into Markdown and Rust. The independent verifier reconstructs the same products without trusting ggen.

## Receipt v2

The evidence digest binds:

`R_pack = H(TTL, Q, T, O*, outputs, utility, tests, checkpoint_ocel, R_prev)`

The runtime boundary requires BLAKE3. SHA-256 is emitted only as a universally available diagnostic checksum. A verifier that cannot execute BLAKE3 reports `UNSUPPORTED`; it must not silently substitute another algorithm.

Receiver evidence contains:

- all 22 gate identifiers;
- the observed state of `GALL-013` through `GALL-022`;
- 20 standards, 18 typed refusals, 10 checkpoints, and nine generated outputs;
- exact candidate-head identity;
- deterministic OCEL 2.0 checkpoint events;
- receipt replay closure.

## Falsifiers

The sabotage suite proves refusal of ontology, template, generated-byte, authority, checkpoint-totality, dependency, standing, pin, OCEL-schema, exact-head, and receipt-replay mutations.

## Constitutional exclusions

The cell does not claim that arbitrary irreversible systems are reversible. It preserves reversibility while exploring graph-domain candidates, then permits bounded filesystem writes only through the verifier's atomic broker. Hooks remain intent-only. Network and arbitrary shell actuation are outside the admitted boundary.

Checkpoint completion is not crown completion. A checkpoint can be `ALIVE` only from observed execution; merger authorization remains a separate actuation decision.

## Standing

A local semantic and sabotage run may prove the Python cell and negative witnesses `ALIVE`. Rust compilation, real ggen execution, BLAKE3 receipt closure, exact-head admission, and artifact publication require their executors to be observed in CI.
