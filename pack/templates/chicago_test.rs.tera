//! Chicago acceptance test for {{ ticket_id }} — generated by chicago-tdd-tools ggen pack
//! CANDIDATE: implement {{ module_path }}::{{ fn_name }} until this passes.
//! Status: CANDIDATE

use chicago_tdd_tools::chicago_test;

#[chicago_test(
    ticket      = "{{ ticket_path }}",
    scaffold_fn = "{{ module_path }}::{{ fn_name }}"
)]
fn {{ scenario_slug }}() {
    // Given: {{ given }}
    // When: {{ when }}
    // Then: {{ then }}
    ::chicago_tdd_tools::__runtime::scaffold_pending(
        "{{ ticket_id }}",
        "{{ ticket_path }}",
        "{{ test_path | default(value=ticket_path) }}",
    )
}
