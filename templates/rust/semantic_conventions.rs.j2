// GENERATED BY `weaver registry generate` — do not edit.
// Source registry: registry/otel (manifest: registry/otel/manifest.yaml)
{% for group in ctx.groups %}
/// Weaver semantic-convention group id: `{{ group.id }}`.
/// {{ group.brief }}
pub const {{ group.id | screaming_snake_case }}: &str = "{{ group.name }}";
{% for attribute in group.attributes %}
/// {{ attribute.brief }}
pub const {{ group.id | screaming_snake_case }}_{{ attribute.name | screaming_snake_case }}: &str = "{{ attribute.name }}";
{% endfor %}
{% endfor %}
